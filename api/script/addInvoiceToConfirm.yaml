request_header:
  method: post
  path: /mage/manual/addInvoiceToConfirm
  connection: keep-alive
  timeout: 10
  save_cookie: true
request_body:
  parameter: addInvoiceToConfirm.yaml
  check_body:
    check_json:
      check_type: perfect_match
      expected_code: 200
      expected_result: addInvoiceToConfirm_response.json

